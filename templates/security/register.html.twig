{% extends 'base.html.twig' %}

{% block title 'S\'inscrire' %}

{% block body %}


    <div class="container mt-4">



   

      
    </div>



 <div class="container mt-5 col-sm-3">

        <div class="card text-center text-white bg-dark mb-3 shadow-lg align-items-center">
            <div class="card-body ">
                <h5 class="card-title">Inscription</h5>
                    {{ form_start(form) }}
                    <div class="row mt-4 text-start">
                       {{ form_row(form.username)}}
                    </div>
                    <div class="row mt-3 text-start">
                        {{ form_row(form.password)}}
                    </div>
                    {{ form_widget(form) }}
                    <div class="">
                
                        <button class="btn btn-success mt-4 ">{{ button|default('S\'inscrire') }}</button>
               
                   
                    </div>
                       <div class="">
                        <a href="{{ path('login') }}" class="mt-3 btn btn-outline-success ">Se connecter</a>
                    </div>
       
                {{ form_end(form) }}
              
            
            </div>
        </div>
    </div>
{% endblock %}

